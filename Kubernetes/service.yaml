apiVersion: v1
kind: Service
metadata:
  name: aqt-app
  labels:
    app: aqt-app
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: aqt-app
  type: LoadBalancer
